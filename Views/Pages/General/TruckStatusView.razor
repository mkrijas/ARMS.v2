@inject ITruckService Iservice

<MudDialog>

    <TitleContent>
        <MudText Typo="Typo.h5">@SelectedValue Trucks</MudText>
    </TitleContent>

    <DialogContent>
        <MudTable T="TruckStatusModel"
                  Items="@truckStatus">
            <HeaderContent>
                <MudTh>Reg NO</MudTh>
                <MudTh>Event Time</MudTh>
            </HeaderContent>
            <RowTemplate>
                <MudTd DataLabel="TripNO">@context.RegNo</MudTd>
                <MudTd DataLabel="Driver">@context.EventTime</MudTd>
            </RowTemplate>
            <PagerContent>
                <MudTablePager PageSizeOptions="new int[] { 10, 25, 50, 100 }" />
            </PagerContent>
        </MudTable>
    </DialogContent>

</MudDialog>

@code {
    [CascadingParameter]
    MudDialogInstance MudDialog { get; set; }

    [Parameter]
    public int? BranchID { get; set; }

    [Parameter]
    public string SelectedValue { get; set; }

    private List<TruckStatusModel> truckStatus = new List<TruckStatusModel>();

    protected override async Task OnInitializedAsync()
    {
        truckStatus = Iservice.GetTruckStatusByEvent(BranchID, SelectedValue).ToList();
    }
}
