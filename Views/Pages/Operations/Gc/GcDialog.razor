@attribute [Authorize]

<MudDialog>
    <DialogContent>
        <MudContainer Style="min-height:300px;max-height: 800px;">
            <EditForm id="form" Model="@model" OnValidSubmit="OnValidSubmit">
                <ObjectGraphDataAnnotationsValidator />
                <ValidationSummary />
                @if (IsLoading)
                {
                    <MudProgressCircular Color="Color.Primary" Style="height: 200px; width: 200px; display: block; margin: 150px auto auto auto; " Indeterminate="true" />
                }
                else
                {
                    <MudGrid Spacing="2">
                        <MudItem xs="12" md="6">
                            <MudAutocomplete Clearable="true" Variant="Variant.Outlined" T="OrderModel" Label="Order" ResetValueOnEmptyText="true" ValueChanged="OrderSelected" Value="Order"
                                             SearchFunc="SearchOrders" ToStringFunc="@(e=> e==null?null : $"{e.OrderName}")"></MudAutocomplete>
                        </MudItem>
                        @if (Order?.OrderID > 0)
                        {
                            <MudItem xs="12" md="6">
                                <MudAutocomplete T="RouteModel" Variant="Variant.Outlined" Label="Route" Value="Route" ValueChanged="RouteChanged" ResetValueOnEmptyText="true"
                                                 SearchFunc="SearchRoutes" ToStringFunc="@(e => e == null ? null : $"{e.RouteName}")"></MudAutocomplete>
                            </MudItem>
                            <MudItem xs="12" md="6">
                                <MudAutocomplete T="ConsigneeModel" Variant="Variant.Outlined" Label="Consignor" Value="Consignor" ValueChanged="ConsignorChanged" ResetValueOnEmptyText="true"
                                                 SearchFunc="SearchConsignor" ToStringFunc="@(e => e == null ? null : $"{e.ConsigneeName}")"
                                                 Adornment="Adornment.End" AdornmentIcon="@Icons.Material.Filled.Add" OnAdornmentClick="AddConsignee_Consignor" AdornmentAriaLabel="Add Consignee Consignor">
                                </MudAutocomplete>
                            </MudItem>
                            <MudItem xs="12" md="6">
                                <MudAutocomplete T="ConsigneeModel" Variant="Variant.Outlined" Label="Consignee" Value="Consignee" ValueChanged="ConsigneeChanged" ResetValueOnEmptyText="true"
                                                 SearchFunc="SearchConsignee" ToStringFunc="@(e => e == null ? null : $"{e.ConsigneeName}")"
                                                 Adornment="Adornment.End" AdornmentIcon="@Icons.Material.Filled.Add" OnAdornmentClick="AddConsignee_Consignor" AdornmentAriaLabel="Add Consignee Consignor"></MudAutocomplete>
                            </MudItem>
                            <MudItem xs="6">
                                @* <MudDatePicker @bind-Date="@model.OrderTime" Label="Order Time" Variant="Variant.Outlined" Margin="Margin.Dense" For="@(() => model.OrderTime)" Editable="true"></MudDatePicker> *@
                                <MudTextField Label="Order Time" Format="s" Variant="Variant.Outlined" @bind-Value="@model.OrderTime" InputType="InputType.DateTimeLocal"></MudTextField>
                            </MudItem>
                            <MudItem xs="12">
                                <MudTabs @bind-ActivePanelIndex="@_tabIndex" Rounded="true" ApplyEffectsToContainer="true">
                                    <ChildContent>
                                        @foreach (var wrapperitem in model.Gcs.Select((value, i) => new { i, value }))
                                        {
                                            var item = wrapperitem.value;
                                            string head = item.GcID > 0 ? item.GcPrefix + item.GcNumber : "New " + (wrapperitem.i + 1);
                                            <MudTabPanel Text="@head" Tag="@item">
                                                <MudGrid Spacing="2">
                                                    <MudItem xs="12" md="4">
                                                        <MudDatePicker Label="Bill Date" Variant="Variant.Outlined" Margin="Margin.Dense" DisableToolbar="true" Editable="true" @bind-Date="item.BillDate" HelperText="Enter Date" DateFormat="dd/MM/yyyy" />
                                                    </MudItem>
                                                    <MudItem xs="12" md="4">
                                                        <MudTextField @bind-Value="item.BillNumber" Label="Bill Number" Variant="Variant.Outlined" Margin="Margin.Dense" For="@(() => item.BillNumber)"></MudTextField>
                                                    </MudItem>
                                                    <MudItem xs="12" md="4">
                                                        <MudTextField Value="item.BillQuantity" T="decimal?" ValueChanged="((e)=>QtyChanged(e,item))" Label="Bill Quantity" Variant="Variant.Outlined" Margin="Margin.Dense" For="@(() => item.BillQuantity)" Adornment="Adornment.End" AdornmentText="@(Order?.Content?.PrimaryUnit)"> </MudTextField>
                                                    </MudItem>
                                                    <MudItem xs="12" md="3">
                                                        <MudSelect Label="Select Type" @bind-Value="item.GcType" Variant="Variant.Outlined" Margin="Margin.Dense"  AdornmentColor="Color.Secondary">
                                                            @foreach (var ditem in GcTypes)
                                                            {
                                                                <MudSelectItem Value="@ditem.GcTypeID">@ditem.GcTypeName</MudSelectItem>
                                                            }
                                                        </MudSelect>
                                                    </MudItem>
                                                    <MudItem xs="12" md="3">
                                                        <MudTextField @bind-Value="item.PassNumber" Label="Pass Number" Variant="Variant.Outlined" Margin="Margin.Dense" For="@(() => item.PassNumber)"></MudTextField>
                                                    </MudItem>
                                                    <MudItem xs="12" md="3">
                                                        <MudTextField @bind-Value="item.LRNo" Label="LR Number" Variant="Variant.Outlined" Margin="Margin.Dense" For="@(() => item.LRNo)"></MudTextField>
                                                    </MudItem>
                                                    <MudItem xs="12" md="3">
                                                        <MudTextField Value="item.Freight" T="decimal?" ValueChanged="((e)=>FrChanged(e,item))" Label="FREIGHT" Variant="Variant.Outlined" Margin="Margin.Dense" For="@(() => item.Freight)"></MudTextField>
                                                    </MudItem>
                                                </MudGrid>
                                            </MudTabPanel>
                                        }
                                    </ChildContent>
                                    <Header Context="HeaderContext">
                                        <MudButtonGroup>
                                            <MudTooltip Text="Add More Invoices">
                                                <MudButton EndIcon="@Icons.Material.Filled.Add" OnClick="@( () => AddMoreInvoice() )">ADD</MudButton>
                                            </MudTooltip>
                                        </MudButtonGroup>
                                    </Header>
                                    <TabPanelHeader Context="PanelHeadContext">
                                        @if (model.Gcs.Count > 1)
                                        {
                                            <MudTooltip Text="Remove Tab">
                                                <MudIconButton Class="ml-2 pa-1" Color="Color.Dark" Icon="@Icons.Material.Filled.Close" OnClick="(() => RemoveInvoice(PanelHeadContext))" />
                                            </MudTooltip>
                                        }
                                    </TabPanelHeader>
                                </MudTabs>
                            </MudItem>
                        }
                    </MudGrid>
                }
            </EditForm>
        </MudContainer>
    </DialogContent>
    <DialogActions>
        @if (model.TotalFreight != 0)
        {
            <MudText Typo="Typo.body1" Class="d-flex mx-4 align-center">
                Total Freight :<MudText Typo="Typo.h6" Color="Color.Primary"> @model.Gcs.Sum(x => x.Freight)?.ToString("C2")</MudText>
            </MudText>
            <MudRadioGroup T="byte?" @bind-SelectedOption="@model.PaidBy" Class="pl-5">
                <MudRadio T="byte?" Option="1" Color="Color.Primary" Dense="true">Paid by Client</MudRadio>
                <MudRadio T="byte?" Option="2" Color="Color.Secondary" Dense="true">Paid by Consignee</MudRadio>
            </MudRadioGroup>
        }
        <MudSpacer></MudSpacer>
        <MudButton OnClick="Cancel">Cancel</MudButton>
        <MudButton Disabled="@_busy" form="form" Variant="Variant.Filled" Color="Color.Success" ButtonType="ButtonType.Submit">Save Gc</MudButton>
        @if(model.GcSetID is null)
        {
            <MudButton Disabled="@_busy"  Variant="Variant.Filled" Color="Color.Error" OnClick="@Delete" ButtonType="ButtonType.Button">Delete</MudButton>
        }
    </DialogActions>
</MudDialog>