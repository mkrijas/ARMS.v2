@using Views.Pages.Operations.Gc
@inject IDialogService DialogService
@inject IGcService Iservice
@inject MudBlazor.ISnackbar snackbar
@attribute [Authorize]
@inject AuthenticationStateProvider auth

@inject NavigationManager UriHelper

<MudDialog>
    <DialogContent>

<MudGrid Spacing="4">

    @foreach (var item in collection)
    {
        <MudItem xs="12" md="12" xl="12">
           <GcItemCard item="item" unloading=true></GcItemCard> 
        </MudItem>
    }

</MudGrid>
</DialogContent>
<DialogActions>
        <MudButton @onclick="UpdateQuantity" Color="Color.Success">Update Quantity</MudButton>

</DialogActions>
</MudDialog>

@code {
    [Parameter]
    public long TripID { get; set; } 
    
    private List<GcSetModel> collection = new();
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }
    protected override void OnInitialized()
    {
        collection = Iservice.SelectedUnloadEvent(TripID);
    }

   
    public void UpdateQuantity()
    {
        
        MudDialog.Close(collection);

    }

  }
