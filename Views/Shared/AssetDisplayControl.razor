@using Views.Pages.Assets
@*@inject MudBlazor.ISnackbar snackbar

@inject AuthenticationStateProvider auth
@attribute [Authorize]*@
@inject IDialogService DialogService


<MudItem xs="12">
    
    <MudGrid Spacing="4">
        @* <div class="col-12" class=" m-0 p-1 border border-secondary">*@

        @* <MudTable Hover="true" Breakpoint="Breakpoint.Sm" FixedHeader="true"
        Items="@simpleAssets"
        Dense="true">

        <HeaderContent>

        <MudTh>Description</MudTh>
        <MudTh>SerialNumber</MudTh>
        <MudTh>AssetCode</MudTh>
        <MudTh>NatureOfAsset</MudTh>
        <MudTh></MudTh>

        </HeaderContent>

        <RowTemplate>
        <MudTd DataLabel="Nr">@context.Description</MudTd>
        <MudTd DataLabel="Sign">@context.SerialNumber</MudTd>
        <MudTd DataLabel="Name">@context.AssetCode</MudTd>
        <MudTd DataLabel="Group">@context.NatureOfAsset</MudTd>
        <MudTd DataLabel="">
        @if (this.hasChildren)
        {

        <MudFab @onclick="ToggleShow"  Icon="@Icons.Material.Filled.ArrowDownward" Size="Size.Small" IconSize="Size.Small" />

        }

        <MudFab @onclick="@(()=> OpenDialogComplexAsset(context))" Color="Color.Primary" Icon="@Icons.Material.Filled.Add" Size="Size.Small" IconSize="Size.Small" />
        </MudTd>
        </RowTemplate>

        </MudTable>
        </div>*@
        <MudItem xs="12" md="1">
            @if (this.hasChildren)
            {

               @* <button class="btn @this.buttonCss" type="button" @onclick="ToggleShow">
                    @buttonText
                </button>*@

                      @* <MudFab @onclick="ToggleShow"   Icon="@Icons.Material.Filled.ArrowDownward" Size="Size.Small" IconSize="Size.Small" />
*@
<MudIconButton Icon="@Icons.Material.Filled.ArrowDownward" @onclick="ToggleShow" Size="Size.Small" IconSize="Size.Small"></MudIconButton>
            }
        </MudItem>
       <MudItem xs="12" md="3">
       
           

        <MudTextField ReadOnly="true" @bind-Value="@this.Model.Parent.Description" Variant="Variant.Outlined" Margin="Margin.Dense"></MudTextField>
           
          
    @*    <MudText> @this.Model.Parent.Description</MudText>*@
     
         
       

        </MudItem> 
         <MudItem xs="12" md="3">                             
        <MudTextField ReadOnly="true" @bind-Value="@this.Model.Parent.NatureOfAsset" Variant="Variant.Outlined" Margin="Margin.Dense"></MudTextField>
           
       @* <MudText> @this.Model.Parent.NatureOfAsset</MudText>   *@
       
        </MudItem>
        <MudItem xs="12" md="3">                             
           
       @* <MudText> @this.Model.Parent.SerialNumber</MudText>   *@
        <MudTextField ReadOnly="true" @bind-Value="@this.Model.Parent.SerialNumber" Variant="Variant.Outlined" Margin="Margin.Dense"></MudTextField>
        
        </MudItem>
        <MudItem xs="12" md="2">
       @* @if (this.hasChildren)
        {
            
                <button class="btn @this.buttonCss" type="button" @onclick="ToggleShow">
                    @buttonText
                </button>

            

        }*@
        <MudTooltip Text="Add Simple Assets">  
        <MudFab @onclick="@(()=> OpenDialogComplexAsset(this.Model.Parent))" Color="Color.Primary" Icon="@Icons.Material.Filled.Add" Size="Size.Small" IconSize="Size.Small" />
</MudTooltip>
        @if (this.Model.Parent.ParentAssetID==null)
            {
                 <MudTooltip Text="Move Asset">  
        <MudFab @onclick="@(()=> MoveAsset(this.Model.Parent))" Color="Color.Primary" Icon="@Icons.Material.Filled.MoveUp" Size="Size.Small" IconSize="Size.Small" />
</MudTooltip>
            }
        </MudItem>
        @* <div class="col-12">
        <MudDataGrid Items="@Model.Children">
        <Columns>
        <Column T="AssetModel" Field="Description" Title="Ts"/>
        <Column T="AssetModel" Field="AssetCode" Title="ms"/>
        <Column T="AssetModel" Field="SerialNumber" Title="ls" />
        <Column T="AssetModel" Field="NatureOfAsset" Title="gs"/>

        </Columns>
        </MudDataGrid>
        </div>*@
    </MudGrid>
    @if (this.Show)
    {
        <div class="row">
            <div class="col-12">
                @if (this.hasChildren)
                {
                    @foreach (var child in this.Model.Children)
                    {

                        <AssetDisplayControl Model="child"></AssetDisplayControl>
                    }
                }
            </div>
        </div>
    }
   
</MudItem>

@code {
    //private List<AssetModel> simpleAssets = new List<AssetModel>();
    [Parameter] public AssetViewModel Model { get; set; }
    DialogOptions maxWidth = new DialogOptions() { MaxWidth = MaxWidth.Medium, FullWidth = true };

    private bool Show;

    private void ToggleShow()
        => this.Show = !this.Show;

    private bool hasChildren => this.Model?.Children?.Count > 0;

    private string buttonText => this.Show ? "Hide" : "Show";
   
    private string buttonCss => this.Show ? "btn-dark" : "btn-primary";

    private string HeaderCols => this.hasChildren ? "col-11" : "col-12";
    protected override async Task OnInitializedAsync()
    {


        // simpleAssets.Add(Model.Parent);
    }
    private async Task OpenDialogComplexAsset(AssetModel tds)
    {
        AssetModel assetModel = new AssetModel();

        assetModel.IsComplex = false;
        if (tds.AssetID != null)
        {
            assetModel.ParentAssetID = tds.AssetID;
        }

        DialogParameters parms = new DialogParameters();
        parms.Add("model", assetModel);
        var dialog = DialogService.Show<AssetSimpleDialog>(assetModel.AssetID == null ? "ADD" : "EDIT", parms, maxWidth);
        var result = await dialog.Result;
        if (!result.Cancelled)
        {
            //await GetAll();
        }

    }
     private async Task MoveAsset(AssetModel tds)
    {
       
        DialogParameters parms = new DialogParameters();
        parms.Add("model", tds);
        var dialog = DialogService.Show<AssetMoveDialog>(tds.AssetID == null ? "ADD" : "EDIT", parms, maxWidth);
        var result = await dialog.Result;
        if (!result.Cancelled)
        {
            //await GetAll();
        }

    }
}